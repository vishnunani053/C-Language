
#include <stdio.h>

int main()
{
 
char  c;
short s;
printf("The size of the variable byte(s) is :%d \n",sizeof(c)); 
printf("The size of the variable byte(c) is :%d \n",sizeof(c)); 
printf("The size of the variable byte(s) is :%d \n",sizeof(float)); 
// printf("The size of the variable byte(s) is :%d \n";sizeof(double));

    return 0;
}